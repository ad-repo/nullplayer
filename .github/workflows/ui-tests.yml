name: Tests

on:
  push:
    branches: [main, test-dev, fixes]
  pull_request:
    branches: [main]

# Note: Unit tests removed — the NullPlayer SPM target pulls in SQLite.swift
# which does not compile reliably on CI (macOS-14/Xcode 16.x environment).
# UI tests must be run locally using Xcode:
#   1. Open Package.swift in Xcode
#   2. Run NullPlayerUITests target with Cmd+U
